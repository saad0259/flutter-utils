import 'package:flutter/material.dart';

regularWhiteText8(color, {fontStyle}) => TextStyle(
      fontSize: 8,
      color: color,
      fontWeight: FontWeight.normal,
      fontStyle: fontStyle,
    );

regularWhiteText10(color, {fontStyle, fontWeight = FontWeight.normal}) =>
    TextStyle(
      fontSize: 10,
      color: color,
      fontWeight: fontWeight,
      fontStyle: fontStyle,
    );

regularWhiteText9(color, {fontStyle}) => TextStyle(
      fontSize: 9,
      color: color,
      fontWeight: FontWeight.normal,
      fontStyle: fontStyle,
    );

regularWhiteText12(color, {fontStyle, fontWeight = FontWeight.normal}) =>
    TextStyle(
        fontSize: 12,
        color: color,
        fontWeight: fontWeight,
        fontStyle: fontStyle,
        decorationThickness: 0);
regularWhiteText13(color, {fontStyle, fontWeight = FontWeight.normal}) =>
    TextStyle(
      fontSize: 13,
      color: color,
      fontWeight: fontWeight,
      fontStyle: fontStyle,
    );
regularWhiteText14(color, {fontStyle, fontWeight = FontWeight.normal}) =>
    TextStyle(
      fontSize: 14,
      color: color,
      fontWeight: fontWeight,
      fontStyle: fontStyle,
      decorationThickness: 0,
    );
regularText15(color, {fontStyle, fontWeight = FontWeight.normal}) =>
    TextStyle(
          fontSize: 15,
          color: color,
          fontWeight: fontWeight,
          fontStyle: fontStyle,
          decorationThickness: 0,
    );
regularWhiteTextwithPupins14(color,
        {fontStyle, fontWeight = FontWeight.normal}) =>
    TextStyle(
        fontSize: 14,
        color: color,
        letterSpacing: -1.0,
        fontWeight: fontWeight,
        fontStyle: fontStyle,
        decorationThickness: 0);
regularWhiteText15(
  color, {
  fontStyle,
  fontWeight = FontWeight.normal,
}) =>
    TextStyle(
      fontSize: 15,
      color: color,
      fontWeight: fontWeight,
      fontStyle: fontStyle,
      decoration: TextDecoration.underline,
    );
regularWhiteText11(color, {fontStyle, fontWeight = FontWeight.normal}) =>
    TextStyle(
      fontSize: 11,
      color: color,
      fontWeight: fontWeight,
      fontStyle: fontStyle,
    );

regularWhiteTextWithDecoration14(color, {fontStyle}) => TextStyle(
    decoration: TextDecoration.underline,
    fontSize: 14,
    color: color,
    fontWeight: FontWeight.normal,
    fontStyle: fontStyle);
regularWhiteTextWithDecoration12(color, {fontStyle}) => TextStyle(
    decoration: TextDecoration.underline,
    decorationColor: Colors.white,
    decorationThickness: 1.0,
    fontSize: 12,
    color: color,
    fontWeight: FontWeight.normal,
    fontStyle: fontStyle);

regularWhiteText16(color,
        {fontStyle, fontWeight = FontWeight.normal, letterSpacing}) =>
    TextStyle(
        fontSize: 16,
        color: color,
        fontWeight: fontWeight,
        fontStyle: fontStyle,
        letterSpacing: letterSpacing);
regularWhiteTextwithpoppins14(color,
        {fontStyle, fontWeight = FontWeight.normal, letterSpacing}) =>
    TextStyle(
        fontSize: 14,
        color: color,
        fontWeight: fontWeight,
        fontStyle: fontStyle,
        letterSpacing: letterSpacing);
regularWhiteTextwithpoppins18(color,
        {fontStyle, fontWeight = FontWeight.normal, letterSpacing}) =>
    TextStyle(
        fontSize: 18,
        color: color,
        fontWeight: FontWeight.w500,
        fontStyle: fontStyle,
        letterSpacing: letterSpacing);
regularWhiteTextwithpoppins16(color,
        {fontStyle, fontWeight = FontWeight.normal, letterSpacing}) =>
    TextStyle(
        fontSize: 16,
        color: color,
        fontWeight: fontWeight,
        fontStyle: fontStyle,
        letterSpacing: letterSpacing);
regularWhiteText17(color,
        {fontStyle, fontWeight = FontWeight.normal, letterSpacing}) =>
    TextStyle(
      fontSize: 17,
      color: color,
      fontWeight: fontWeight,
      letterSpacing: letterSpacing,
      fontStyle: fontStyle,
    );

regularWhiteText18(color,
        {fontStyle, fontWeight = FontWeight.normal, letterSpacing}) =>
    TextStyle(
      fontSize: 18,
      color: color,
      fontWeight: fontWeight,
      letterSpacing: letterSpacing,
      fontStyle: fontStyle,
    );
regularWhiteText19(color,
        {fontStyle, fontWeight = FontWeight.normal, letterSpacing}) =>
    TextStyle(
      fontSize: 19,
      color: color,
      fontWeight: fontWeight,
      letterSpacing: letterSpacing,
      fontStyle: fontStyle,
    );
regularWhiteText18WithLineSpace24(color, {fontStyle}) => TextStyle(
    fontSize: 18,
    color: color,
    fontWeight: FontWeight.normal,
    height: 2.4,
    fontStyle: fontStyle);
regularWhiteText22(color,
        {fontStyle, fontWeight = FontWeight.normal, letterSpacing}) =>
    TextStyle(
      fontSize: 22,
      color: color,
      fontWeight: fontWeight,
      letterSpacing: letterSpacing,
      fontStyle: fontStyle,
    );
regularWhiteText20(color, {fontStyle, fontWeight = FontWeight.normal}) =>
    TextStyle(
      fontSize: 20,
      color: color,
      fontWeight: fontWeight,
      fontStyle: fontStyle,
    );
regularWhiteText24(color, {fontStyle, fontWeight = FontWeight.normal}) =>
    TextStyle(
      fontSize: 24,
      color: color,
      fontStyle: fontStyle,
      fontWeight: fontWeight,
    );
regularWhiteText28(color,
        {fontStyle, fontWeight = FontWeight.normal, letterSpacing}) =>
    TextStyle(
        fontSize: 28,
        color: color,
        fontWeight: fontWeight,
        fontStyle: fontStyle,
        letterSpacing: letterSpacing);
regularWhiteText26(color,
        {fontStyle, fontWeight = FontWeight.normal, letterSpacing}) =>
    TextStyle(
      fontSize: 26,
      color: color,
      letterSpacing: letterSpacing,
      fontWeight: fontWeight,
      fontStyle: fontStyle,
    );

regularWhiteText34(color,
        {fontStyle, fontWeight = FontWeight.normal, letterSpacing}) =>
    TextStyle(
      fontSize: 34,
      color: color,
      letterSpacing: letterSpacing,
      height: 1.2,
      fontWeight: fontWeight,
      fontStyle: fontStyle,
    );
regularWhiteText(color, {fontStyle, fontSize, double letterSpace = 2.9}) =>
    TextStyle(
      fontSize: fontSize,
      color: color,
      fontWeight: fontStyle,
      letterSpacing: letterSpace,
    );
//Bold Text Styles
